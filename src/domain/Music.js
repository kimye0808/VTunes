export default class Music {
  constructor(name, lyrics, duration, artist, album) {
    this.name = name;
    this.lyrics = lyrics;
    this.duration = duration;
    this.artist = artist;
    this.album = album;
  }
}